<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <!-- Basic file set properties -->
    <property name="charset" value="UTF-8"/>

    <!-- Suppressions can be added via config/suppressions.xml if needed -->

    <!-- TreeWalker contains the bulk of checks for Java source files -->
    <module name="TreeWalker">
        <!-- Avoid wildcard imports -->
        <module name="AvoidStarImport"/>

        <!-- Enforce Javadoc on public methods/classes -->
        <module name="JavadocType"/>
        <module name="JavadocMethod"/>

        <!-- Naming conventions -->
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="ParameterName"/>

        <!-- Misc style checks -->
        <module name="FinalLocalVariable"/>
        <module name="UnusedImports"/>
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
        </module>
    </module>

    <!-- LineLength must be a child of Checker (not TreeWalker) in newer Checkstyle versions -->
    <module name="LineLength">
        <property name="max" value="160"/>
    </module>

    <!-- File tab/charset checks -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>
</module>
